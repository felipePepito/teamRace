{% extends '::TeamRace.html.twig' %}

{% block navigation %}

<a id="navigationHome" href="{{ url('externalIndex') }}">teamrace</a><span class="navigationSpacer"></span>
<a class="navigationItem" href="{{ url('externalAbout') }}">{{ "about" | trans }}</a><span class="navigationSpacer"></span>
<a class="navigationItem" href="{{ url('login') }}">{{ "login" | trans }}</a><span class="navigationSpacer"></span>
<a class="navigationItem" href="{{ url('externalRegister') }}">{{ "register" | trans }}</a><span class="navigationSpacer"></span>

<span class="navigationSpacer"></span>
<span class="navigationSpacer"></span>
<span class="navigationSpacer"></span>

<span id="navigationLocale">
	{{ "Choose language:"|trans }}
	&nbsp;
	<a class="navigationLocaleSwitch" id="de" href="{{ url('languageSwitch', {'locale': 'de'}) }}">de</a>
	<a class="navigationLocaleSwitch" id="en" href="{{ url('languageSwitch', {'locale': 'en'}) }}">en</a>
</span>

{% endblock %}